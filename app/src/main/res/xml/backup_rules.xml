<?xml version="1.0" encoding="utf-8"?>
<!-- Backups are off in the manifest; this file exists so the reference resolves. -->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="datastore" />
</full-backup-content>
